# ============================================
# OpenAI / 大模型配置
# 支持 OpenAI 官方 API 及兼容接口（DeepSeek、智谱、通义等）
# 如果直连不了 OpenAI，设置 OPENAI_PROXY
# ============================================
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=local
OPENAI_TIMEOUT=30
OPENAI_PROXY=

# ============================================
# HuggingFace 配置（本地语义嵌入 + 重排序模型）
# ============================================
# 国内用户建议设置镜像站加速下载（留空则默认访问 huggingface.co）
HF_ENDPOINT=https://hf-mirror.com

# 本地语义嵌入模型（中文推荐 BAAI/bge-small-zh-v1.5）
LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 重排序模型（提升检索精度，首次运行自动下载 ~1GB）
RERANKER_MODEL=BAAI/bge-reranker-base

# 对话记忆有效期（秒），超时自动清除
CONVERSATION_TTL_SECONDS=3600

# ============================================
# MySQL 数据库配置
# ============================================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your-password-here
MYSQL_DATABASE=smart_doc_qa

# ============================================
# Redis 配置（可选，用于缓存和任务队列）
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# 应用配置
# ============================================
APP_NAME=SmartDocQA
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DEBUG=true

# 向量存储路径（默认为项目根目录下的 data/vector_store，一般不需要修改）
# VECTOR_STORE_PATH=

# 支持的文件类型
ALLOWED_EXTENSIONS=.pdf,.txt,.md,.docx,.csv

# 文件大小限制 (MB)
MAX_FILE_SIZE_MB=10

# 文本分块大小
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
